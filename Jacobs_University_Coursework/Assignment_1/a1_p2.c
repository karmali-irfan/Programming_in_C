#include <stdio.h>

/*
CH-230-A
a1_p2.c
Irfan Karmali 
i.karmali@jacobs-university.de 
*/

int main () {

    int result; /*The result of the calculation*/
    result = (2 + 7) * 9/3 ;
    printf("The result is %d\n", result);
    return 0;

    /* The programs reads the '%d' command and knows to insert an integer but since we didn't specify what integer, the program just inserts a random integer
    to fulfill that parameter*/   
}

